{
  "id": "cs146/2-adts-invariants-insertion",
  "course": "cs146",
  "lessons": "Lectures 2–3",
  "title": "ADTs, loop invariants, and insertion sort",
  "summary": "Say what an ADT is and pick stack vs queue for a scenario; write an array stack and a circular queue; draw a singly linked list node-by-node and splice into it without losing the tail; prove a loop correct with a three-part invariant; trace insertion sort the way Poon grades it and explain its best and worst case.",
  "estimatedMinutes": 75,
  "sourceNote": "Lecture 2 (Aug 24) and Lecture 3 (Aug 26) slides, HW 2, HW 3, Project 1 Part 1.",
  "requires": [],
  "sections": [
    {
      "id": "why",
      "heading": "Why this course starts with 'what does it do', not 'how'",
      "blocks": [
        {
          "type": "prose",
          "label": "why",
          "md": "**The whole lecture in one idea** You can drive a car without knowing whether the engine is gas or electric. The pedals are a **contract**: press this, the car goes. An **Abstract Data Type** is a contract like that for data: it lists the operations you can do (push, pop, peek…) and what they promise, and says nothing about how they're built inside. The 'how' is the **implementation** — and the same contract can have several implementations with very different speeds. Almost every exam question in this course is, underneath, 'which contract does this problem need, and which implementation makes those operations cheap?'",
          "slide": "The car-pedal idea",
          "id": "why.30f9d804"
        },
        {
          "type": "prose",
          "md": "**Where you'll meet this after the exam** Every language ships these contracts as libraries, and picking the right one IS the job. In Java: `ArrayDeque` when you need a stack or queue, `ArrayList` when you need a list, and `Stack` exists but is legacy — knowing *why* (it locks on every call) is exactly the ADT-vs-implementation distinction. In interviews, half the medium questions are a story like the tunnel problem below: the interviewer never says the word 'stack'; they describe a behavior and watch whether you hear LIFO or FIFO in it. And in real systems: your editor's undo, the browser's back button, a printer's job list, and the task queue behind every web server are these four contracts, deployed billions of times.",
          "id": "why.7c3e91a2"
        },
        {
          "type": "trap",
          "body": "**Poon's definition (reproduce it)** 'An abstract data type is a data type defined by its behavior — what it does — not its implementation — how it does it.' He wants that sentence, then an example. The pedal analogy is his.",
          "source": "Exam",
          "points": null,
          "id": "why.42b4062a"
        },
        {
          "type": "definition",
          "term": "ADT · List · Stack · Queue",
          "body": "**List**: a sequence you access by position (index). Operations: add, remove, get, size. **Stack**: last in, first out (LIFO). push, pop, peek, isEmpty. Plates in a cafeteria. **Queue**: first in, first out (FIFO). enqueue, dequeue, peek, isEmpty. A checkout line.",
          "id": "why.14b9fb81"
        }
      ]
    },
    {
      "id": "stack",
      "heading": "Stacks: LIFO, and what push/pop actually do",
      "blocks": [
        {
          "type": "prose",
          "md": "Your browser's back button is a stack: every page you visit is pushed; Back pops. Undo is a stack. And the big one: **the call stack** — every time a function calls another, the new call is pushed on top; when it returns, it's popped. That's why recursion works at all, and why infinite recursion gives a 'stack overflow' error: the stack literally ran out of room.",
          "slide": "Where stacks show up",
          "id": "stack.778c1ffc"
        },
        {
          "type": "stepper",
          "title": "An array stack, operation by operation",
          "frames": [
            {
              "kind": "array",
              "cells": [
                "·",
                "·",
                "·",
                "·"
              ],
              "note": "top = −1  (empty)",
              "caption": "An array stack with room for 4. `top` is the index of the most recent item. −1 means nothing is there yet. Reading `a[top]` now would be an **underflow** error."
            },
            {
              "kind": "array",
              "cells": [
                5,
                "·",
                "·",
                "·"
              ],
              "hl": [
                0
              ],
              "note": "push(5): top = 0",
              "caption": "push(5): move `top` up by one, then write 5 there. Two steps, no loops, no matter how full the stack is — that's O(1)."
            },
            {
              "kind": "array",
              "cells": [
                5,
                9,
                "·",
                "·"
              ],
              "hl": [
                1
              ],
              "note": "push(9): top = 1",
              "caption": "push(9): same two steps. 9 sits on top of 5."
            },
            {
              "kind": "array",
              "cells": [
                5,
                9,
                2,
                "·"
              ],
              "hl": [
                2
              ],
              "note": "push(2): top = 2",
              "caption": "push(2). The last thing in is always at `a[top]`."
            },
            {
              "kind": "array",
              "cells": [
                5,
                9,
                2,
                "·"
              ],
              "hl": [
                2
              ],
              "note": "peek() → 2",
              "caption": "peek(): read `a[top]` and change nothing. Answer 2."
            },
            {
              "kind": "array",
              "cells": [
                5,
                9,
                "2",
                "·"
              ],
              "done": [
                2
              ],
              "hl": [
                1
              ],
              "note": "pop() → 2, top = 1",
              "caption": "pop(): read `a[top]` (2), then move `top` down. The 2 is still physically in the array, but nothing points at it — it's gone as far as the stack is concerned. The next push will overwrite it."
            },
            {
              "kind": "array",
              "cells": [
                5,
                "9",
                "2",
                "·"
              ],
              "done": [
                1,
                2
              ],
              "hl": [
                0
              ],
              "note": "pop() → 9, top = 0",
              "caption": "pop() again gives 9. Last in, first out: we pushed 5, 9, 2 and got back 2, 9. That order reversal is the whole point of a stack."
            }
          ],
          "slide": true,
          "id": "stack.acaab828"
        },
        {
          "type": "example",
          "title": "Java, because that's what Poon's slides and Project 1 use. If you know JS/TS: `int[]` is a fixed-size array of ints, `public` is just visibility, and the semicolons are mandatory.",
          "body": "public class ArrayStack {\n  private int[] a;\n  private int top = -1;              // index of the top item; -1 = empty\n\n  public ArrayStack(int capacity) { a = new int[capacity]; }\n\n  public void push(int x) {\n    if (top == a.length - 1) throw new RuntimeException(\"overflow\");\n    top++;\n    a[top] = x;\n  }\n  public int pop() {\n    if (top == -1) throw new RuntimeException(\"underflow\");\n    int x = a[top];\n    top--;\n    return x;\n  }\n  public int peek()      { return a[top]; }   // same check as pop\n  public boolean isEmpty() { return top == -1; }\n}",
          "slide": "Array stack in Java (write it cold)",
          "id": "stack.c81ce0de"
        },
        {
          "type": "prose",
          "md": "A **linked list** version keeps the top as the head node (the full node-and-arrow picture gets its own section below). push: make a new node, point its next at the old head, make it the new head — that's the two-write splice at the head position. pop: save the head's value, move head to head.next. Same four operations, same O(1) cost. Differences: no overflow (it grows as needed), no fixed capacity, but every node costs an extra pointer of memory and the nodes are scattered around memory instead of side by side. Underflow check becomes `head == null`.",
          "slide": "Linked stack: same contract, different insides",
          "id": "stack.a3187b1b"
        },
        {
          "type": "check",
          "prompt": "You push 1, 2, 3, then pop once, then push 4. What does peek return, and what's the order the remaining items come out in?",
          "answer": "peek → 4. Pops give 4, 2, 1. (3 was popped and is gone.)",
          "sectionRef": "stack",
          "id": "stack.1f743ec1"
        }
      ]
    },
    {
      "id": "queue",
      "heading": "Queues: FIFO, and why the circular buffer exists",
      "blocks": [
        {
          "type": "prose",
          "md": "Anything that must be served in arrival order: print jobs, the operating system deciding which program runs next, messages between servers, breadth-first search later in this course. The contract: **enqueue** at the back, **dequeue** from the front.",
          "slide": "Where queues show up",
          "id": "queue.74e0af0a"
        },
        {
          "type": "prose",
          "label": "why",
          "md": "**Why 'just use an array' is O(n)** Put the front at index 0. Dequeue removes index 0 — and now everything has to shift one slot left to keep the front at 0. For a queue of n items that's n copies for one removal: O(n). Poon calls this the naive array queue. The fix keeps two indexes, `head` and `tail`, and lets the data **wrap around** the end of the array.",
          "slide": "The naive array queue's problem",
          "id": "queue.2e046e32"
        },
        {
          "type": "stepper",
          "title": "The circular array queue, and the moment `%` matters",
          "frames": [
            {
              "kind": "array",
              "cells": [
                "·",
                "·",
                "·",
                "·"
              ],
              "note": "head = 0, tail = −1, size 0",
              "caption": "A queue in an array of capacity 4. `head` is where the next dequeue reads; `tail` is where the last enqueue wrote."
            },
            {
              "kind": "array",
              "cells": [
                "A",
                "B",
                "C",
                "·"
              ],
              "hl": [
                0
              ],
              "done": [
                1,
                2
              ],
              "note": "enqueue A, B, C → tail = 2",
              "caption": "Three enqueues. Each one moves `tail` right by one and writes there. `head` still points at A, the oldest."
            },
            {
              "kind": "array",
              "cells": [
                "A",
                "B",
                "C",
                "·"
              ],
              "hl": [
                1
              ],
              "done": [
                2
              ],
              "note": "dequeue() → A, head = 1",
              "caption": "dequeue(): read `a[head]` (A) and move `head` right. Nothing shifts. The naive version would copy B and C one slot left — O(n) work for one removal. We just moved an index: O(1)."
            },
            {
              "kind": "array",
              "cells": [
                "·",
                "B",
                "C",
                "D"
              ],
              "hl": [
                1
              ],
              "done": [
                2,
                3
              ],
              "note": "enqueue D → tail = 3",
              "caption": "enqueue(D) fills the last slot. The array now looks full — but slot 0 is free. Without a trick, the next enqueue fails even though there's room."
            },
            {
              "kind": "array",
              "cells": [
                "E",
                "B",
                "C",
                "D"
              ],
              "hl": [
                1
              ],
              "done": [
                2,
                3,
                0
              ],
              "note": "enqueue E → tail = (3+1) % 4 = 0",
              "caption": "The trick: `tail = (tail + 1) % capacity`. 4 % 4 = 0, so `tail` wraps around to slot 0 and E goes there. The queue order is still B, C, D, E — it just bends around the end of the array. That's why Poon's efficient queue uses modulo."
            }
          ],
          "slide": true,
          "id": "queue.834809ff"
        },
        {
          "type": "figure",
          "viewBox": "0 0 640 240",
          "svg": "<g font-family='ui-sans-serif, system-ui' fill='currentColor'><defs><marker id='cqA' viewBox='0 0 10 10' refX='9' refY='5' markerWidth='7' markerHeight='7' orient='auto-start-reverse'><path d='M0 0 L10 5 L0 10 z' fill='rgb(var(--accent-fg))'/></marker><marker id='cqG' viewBox='0 0 10 10' refX='9' refY='5' markerWidth='7' markerHeight='7' orient='auto-start-reverse'><path d='M0 0 L10 5 L0 10 z' fill='rgb(var(--on-track))'/></marker></defs><g><title>The array itself: 8 fixed slots that never move and never shift. Green = currently holding queue data (C, D, E and the wrapped F). The faded slots 1–4 aren't empty in memory — they hold stale values from old dequeues, waiting to be overwritten. The queue is just the region between head and tail, read with wraparound.</title><rect x='32' y='70' width='64' height='48' rx='8' fill='rgb(var(--on-track) / 0.12)' stroke='rgb(var(--on-track))' stroke-width='1.6'/><rect x='104' y='70' width='64' height='48' rx='8' fill='none' stroke='currentColor' opacity='0.35'/><rect x='176' y='70' width='64' height='48' rx='8' fill='none' stroke='currentColor' opacity='0.35'/><rect x='248' y='70' width='64' height='48' rx='8' fill='none' stroke='currentColor' opacity='0.35'/><rect x='320' y='70' width='64' height='48' rx='8' fill='none' stroke='currentColor' opacity='0.35'/><rect x='392' y='70' width='64' height='48' rx='8' fill='rgb(var(--on-track) / 0.12)' stroke='rgb(var(--on-track))' stroke-width='1.6'/><rect x='464' y='70' width='64' height='48' rx='8' fill='rgb(var(--on-track) / 0.12)' stroke='rgb(var(--on-track))' stroke-width='1.6'/><rect x='536' y='70' width='64' height='48' rx='8' fill='rgb(var(--on-track) / 0.12)' stroke='rgb(var(--on-track))' stroke-width='1.6'/><text x='64' y='101' text-anchor='middle' font-size='17' font-weight='700'>F</text><text x='424' y='101' text-anchor='middle' font-size='17' font-weight='700'>C</text><text x='496' y='101' text-anchor='middle' font-size='17' font-weight='700'>D</text><text x='568' y='101' text-anchor='middle' font-size='17' font-weight='700'>E</text><text x='64' y='138' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace' opacity='0.55'>0</text><text x='136' y='138' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace' opacity='0.55'>1</text><text x='208' y='138' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace' opacity='0.55'>2</text><text x='280' y='138' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace' opacity='0.55'>3</text><text x='352' y='138' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace' opacity='0.55'>4</text><text x='424' y='138' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace' opacity='0.55'>5</text><text x='496' y='138' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace' opacity='0.55'>6</text><text x='568' y='138' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace' opacity='0.55'>7</text></g><g><title>head = 5: where the NEXT dequeue reads. It only ever moves right (with wraparound) — dequeue never shifts data, it just abandons a slot. That's the whole O(n)-to-O(1) win over the naive queue.</title><text x='424' y='34' text-anchor='middle' font-size='12' font-family='ui-monospace, monospace' fill='rgb(var(--on-track))'>head = 5</text><path d='M424 40 L424 64' stroke='rgb(var(--on-track))' stroke-width='1.6' fill='none' marker-end='url(#cqG)'/></g><g><title>tail = 0: where the LAST enqueue wrote. Yes, zero — the newest item F sits at the array's start even though it's logically last. Position in the array means nothing; only head, tail and the modulo arithmetic define the queue.</title><text x='64' y='34' text-anchor='middle' font-size='12' font-family='ui-monospace, monospace' fill='rgb(var(--accent-fg))'>tail = 0</text><path d='M64 40 L64 64' stroke='rgb(var(--accent-fg))' stroke-width='1.6' fill='none' marker-end='url(#cqA)'/></g><g><title>The wrap: E went into slot 7, so the next enqueue computed (7 + 1) % 8 = 0 and F landed in slot 0. One modulo replaces all shifting — this is the single line of code the whole circular design exists for.</title><path d='M568 126 C 520 192, 112 192, 68 128' stroke='rgb(var(--accent-fg))' stroke-width='1.6' fill='none' marker-end='url(#cqA)'/><text x='318' y='188' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace' fill='rgb(var(--accent-fg))'>(7 + 1) % 8 = 0 — tail wrapped</text></g><g><title>What a consumer actually sees when it drains the queue: start at head and follow the arithmetic — C, D, E, then wrap to F. The physical layout (F first!) never leaks out through the ADT's operations.</title><text x='318' y='222' text-anchor='middle' font-size='11' opacity='0.7'>logical queue order, starting at head: C → D → E → F</text></g></g>",
          "caption": "The stepper's queue, grown to capacity 8 and one wrap later. Green cells are occupied; F physically sits at index 0 even though it's the LAST item, because (7 + 1) % 8 = 0. The queue's logical order starts at head and follows the arithmetic, not the picture: C, D, E, F. Slots 1–4 hold garbage from old dequeues, waiting to be overwritten. Real systems call this exact structure a ring buffer — your keyboard driver, sound card, and network card each run one right now.",
          "slide": "Why it's called circular",
          "id": "queue.b1c2d3e4"
        },
        {
          "type": "example",
          "title": "The `%` keeps head and tail inside 0..capacity−1. `size` tells full from empty (both have head == tail otherwise).",
          "body": "public class ArrayQueue {\n  private int[] a; private int head = 0, tail = -1, size = 0;\n  public ArrayQueue(int capacity) { a = new int[capacity]; }\n\n  public void enqueue(int x) {\n    if (size == a.length) throw new RuntimeException(\"overflow\");\n    tail = (tail + 1) % a.length;    // wrap around\n    a[tail] = x; size++;\n  }\n  public int dequeue() {\n    if (size == 0) throw new RuntimeException(\"underflow\");\n    int x = a[head];\n    head = (head + 1) % a.length;    // wrap around\n    size--; return x;\n  }\n}",
          "slide": "Circular queue in Java",
          "id": "queue.0488b2d9"
        },
        {
          "type": "trap",
          "body": "**The linked-queue bug he calls out** A linked queue keeps `head` (front) and `tail` (back). When you dequeue the **last** item, head becomes null — and you must also set `tail = null`. Forget that and `tail` points at a node that no longer belongs to the queue; the next enqueue attaches to a ghost. He shows this on the slides; it's a likely 'what's wrong with this code' question.",
          "source": "Exam material",
          "points": null,
          "id": "queue.abd2d827"
        },
        {
          "type": "table",
          "columns": [
            "",
            "Array-based",
            "Linked list"
          ],
          "rows": [
            [
              "Get item at index k",
              "O(1) — like numbered lockers",
              "O(n) — walk the train car by car"
            ],
            [
              "Insert / delete in the middle",
              "O(n) — shift everything after",
              "O(1) if you're already holding the node"
            ],
            [
              "Memory",
              "Contiguous, cache-friendly",
              "Extra pointer per node, scattered"
            ],
            [
              "Growth",
              "Fixed capacity; resize (usually double) when full",
              "Grows one node at a time, no overflow"
            ]
          ],
          "slide": "Array vs linked list — the trade-off in one table",
          "id": "queue.fc3d685a"
        },
        {
          "type": "example",
          "title": "Picking the ADT from a story",
          "body": "A single-lane tunnel. Scenario 1: cars leave in the order they entered. Scenario 2: the tunnel is blocked, so the last car in must back out first. Which ADT models each, and which operations are entering and leaving?\n\n1. Ask one question: does the *first* thing in leave first, or the *last*?\n2. Scenario 1: first in, first out → **Queue**. Entering = enqueue at the back. Leaving = dequeue from the front.\n3. Scenario 2: last in, first out → **Stack**. Entering = push. Backing out = pop — the most recently pushed car is the first one popped.",
          "answer": "1: queue (enqueue/dequeue). 2: stack (push/pop).",
          "slide": "HW 2: the single-lane tunnel",
          "id": "queue.1f7a6a6e"
        },
        {
          "type": "check",
          "prompt": "Why does the efficient array queue use `% capacity`?",
          "answer": "So head and tail wrap around to reuse the slots freed at the front. Without it, either the queue walks off the end of the array while empty slots sit unused at the front, or dequeue has to shift every element (O(n)). With it, both operations stay O(1).",
          "sectionRef": "queue",
          "id": "queue.4c9ffc5f"
        }
      ]
    },
    {
      "id": "linked",
      "heading": "Linked lists: boxes, arrows, and addresses",
      "blocks": [
        {
          "type": "prose",
          "label": "why",
          "md": "**The mental model, before any code** An array is a row of numbered lockers: locker 7 is always right next to locker 6, so 'go to locker k' is instant. A **linked list** throws the lockers anywhere in memory and instead gives each box a forwarding address. Every **node** is a little struct with two fields: the **data** it holds, and **next** — the memory address of the following node. The only thing you keep is **head**, the address of the first node. To reach anything else you *follow the arrows*, one hop at a time. That one design choice explains every cost in the comparison table you saw earlier: no jumping to position k (you must walk k hops), but splicing a node in or out is just rewriting two addresses — no shifting, ever.",
          "slide": "Boxes with forwarding addresses",
          "id": "linked.a17e40b3"
        },
        {
          "type": "definition",
          "term": "Node · head · null",
          "body": "**Node**: one box holding `data` and `next` (the address of the following node). **head**: the variable holding the first node's address — lose it and the entire list is unreachable. **null** in a `next` field means 'the chain ends here'; the last node always carries it. An empty list is simply `head == null`.",
          "id": "linked.d41a7b22"
        },
        {
          "type": "figure",
          "viewBox": "0 0 690 320",
          "svg": "<g font-family='ui-sans-serif, system-ui' fill='currentColor'><defs><marker id='llA' viewBox='0 0 10 10' refX='9' refY='5' markerWidth='7' markerHeight='7' orient='auto-start-reverse'><path d='M0 0 L10 5 L0 10 z' fill='currentColor'/></marker><marker id='llH' viewBox='0 0 10 10' refX='9' refY='5' markerWidth='7' markerHeight='7' orient='auto-start-reverse'><path d='M0 0 L10 5 L0 10 z' fill='rgb(var(--accent-fg))'/></marker></defs><g><title>head is not a node — it's one ordinary variable holding one address (0x1A4). Lose this variable and the entire list still exists in memory but nothing can ever reach it again.</title><text x='24' y='34' font-size='12' font-family='ui-monospace, monospace' fill='rgb(var(--accent-fg))'>Node&lt;E&gt; head = 0x1A4;</text><path d='M62 42 C 56 60, 50 74, 46 92' stroke='rgb(var(--accent-fg))' stroke-width='1.6' fill='none' marker-end='url(#llH)'/></g><g><title>Node A, living at address 0x1A4 (the tag underneath). Its data cell holds the value 'A'; its next cell holds 0x2B8 — which is exactly B's address. The arrow is just this number, drawn.</title><text x='55' y='88' text-anchor='middle' font-size='10' opacity='0.6'>data</text><text x='120' y='88' text-anchor='middle' font-size='10' opacity='0.6'>next</text><rect x='28' y='96' width='54' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><rect x='82' y='96' width='76' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><text x='55' y='130' text-anchor='middle' font-size='20' font-weight='700' fill='rgb(var(--on-track))'>A</text><text x='120' y='128' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace'>0x2B8</text><text x='93' y='170' text-anchor='middle' font-size='10' font-family='ui-monospace, monospace' opacity='0.55'>@ 0x1A4</text><path d='M158 123 C 182 123, 186 208, 196 216' stroke='currentColor' stroke-width='1.5' fill='none' marker-end='url(#llA)'/></g><g><title>Node B at 0x2B8 — drawn lower on purpose: in real memory nodes sit wherever the allocator dropped them, not in a tidy row. Only the addresses connect them. B's next (0x47C) points at C.</title><text x='227' y='182' text-anchor='middle' font-size='10' opacity='0.6'>data</text><text x='292' y='182' text-anchor='middle' font-size='10' opacity='0.6'>next</text><rect x='200' y='190' width='54' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><rect x='254' y='190' width='76' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><text x='227' y='224' text-anchor='middle' font-size='20' font-weight='700' fill='rgb(var(--on-track))'>B</text><text x='292' y='222' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace'>0x47C</text><text x='265' y='264' text-anchor='middle' font-size='10' font-family='ui-monospace, monospace' opacity='0.55'>@ 0x2B8</text><path d='M330 217 C 354 217, 356 132, 366 124' stroke='currentColor' stroke-width='1.5' fill='none' marker-end='url(#llA)'/></g><g><title>Node C at 0x47C. To reach it from head you MUST hop A → B → C: three pointer follows. That walk is why get(k) costs O(n) — there is no arithmetic shortcut to the k-th node.</title><text x='397' y='88' text-anchor='middle' font-size='10' opacity='0.6'>data</text><text x='462' y='88' text-anchor='middle' font-size='10' opacity='0.6'>next</text><rect x='370' y='96' width='54' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><rect x='424' y='96' width='76' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><text x='397' y='130' text-anchor='middle' font-size='20' font-weight='700' fill='rgb(var(--on-track))'>C</text><text x='462' y='128' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace'>0x9D0</text><text x='435' y='170' text-anchor='middle' font-size='10' font-family='ui-monospace, monospace' opacity='0.55'>@ 0x47C</text><path d='M500 123 C 524 123, 526 208, 534 216' stroke='currentColor' stroke-width='1.5' fill='none' marker-end='url(#llA)'/></g><g><title>Node D, the tail: its next holds null — the agreed 'chain ends here' value. Every walk of the list stops when it reads null. Forgetting to check for it is the classic NullPointerException.</title><text x='565' y='182' text-anchor='middle' font-size='10' opacity='0.6'>data</text><text x='630' y='182' text-anchor='middle' font-size='10' opacity='0.6'>next</text><rect x='538' y='190' width='54' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><rect x='592' y='190' width='76' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><text x='565' y='224' text-anchor='middle' font-size='20' font-weight='700' fill='rgb(var(--on-track))'>D</text><text x='630' y='222' text-anchor='middle' font-size='12' font-style='italic' opacity='0.8'>null</text><text x='603' y='264' text-anchor='middle' font-size='10' font-family='ui-monospace, monospace' opacity='0.55'>@ 0x9D0</text></g><text x='345' y='302' text-anchor='middle' font-size='11' opacity='0.7'>each next field literally CONTAINS the address written under the node it points to</text></g>",
          "caption": "A singly linked list of A → B → C → D, drawn the way memory actually holds it. Under each node is its own address (@ 0x1A4, @ 0x2B8, …); inside each next cell is the address of the following node — match them and you can see the arrows are just numbers. head is a single variable holding 0x1A4. D's next is null: end of the chain. Note the nodes are deliberately NOT in a straight line — in real memory they're scattered wherever the allocator put them, and only the addresses connect them.",
          "slide": "A singly linked list, drawn honestly",
          "id": "linked.f1a2b3c4"
        },
        {
          "type": "prose",
          "md": "**Where you'll meet this after the exam** More places than any other structure in this course, usually wearing a costume. **Git**: every commit stores the hash of its parent commit — `git log` is literally walking a linked list from head. **Blockchains**: same trick, where 'next' is a cryptographic hash, which is why you can't quietly edit a block in the middle. **Your OS's memory allocator** keeps free chunks of RAM as a linked list threaded through the free memory itself. **LRU caches** (the thing that decides what stays in Redis or your CPU cache) are a linked list plus a hash map — move-to-front is O(1) precisely because splicing is two pointer writes. And **the trap**: in interviews, 'reverse a linked list' and 'detect a cycle' are rites of passage because they test whether you can rewire next pointers without dropping the chain — exactly the skill the next figure drills.",
          "slide": "Git, blockchains, allocators, LRU",
          "id": "linked.3e8b91c7"
        },
        {
          "type": "figure",
          "viewBox": "0 0 640 310",
          "svg": "<g font-family='ui-sans-serif, system-ui' fill='currentColor'><defs><marker id='llS' viewBox='0 0 10 10' refX='9' refY='5' markerWidth='7' markerHeight='7' orient='auto-start-reverse'><path d='M0 0 L10 5 L0 10 z' fill='rgb(var(--accent-fg))'/></marker><marker id='llR' viewBox='0 0 10 10' refX='9' refY='5' markerWidth='7' markerHeight='7' orient='auto-start-reverse'><path d='M0 0 L10 5 L0 10 z' fill='rgb(var(--critical))'/></marker></defs><g><title>Node B: after the splice its next cell holds 0x9E0 — X's address. This is the ONLY field of B that changes; B never moves, nothing shifts.</title><text x='57' y='52' text-anchor='middle' font-size='10' opacity='0.6'>data</text><text x='122' y='52' text-anchor='middle' font-size='10' opacity='0.6'>next</text><rect x='30' y='60' width='54' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><rect x='84' y='60' width='76' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><text x='57' y='94' text-anchor='middle' font-size='20' font-weight='700' fill='rgb(var(--on-track))'>B</text><text x='122' y='92' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace'>0x9E0</text><text x='95' y='134' text-anchor='middle' font-size='10' font-family='ui-monospace, monospace' opacity='0.55'>@ 0x2B8</text></g><g><title>Node C at 0x47C — completely untouched by the whole operation. It doesn't know or care that a new node now points at it; C still carries the rest of the chain behind it.</title><text x='503' y='52' text-anchor='middle' font-size='10' opacity='0.6'>data</text><text x='568' y='52' text-anchor='middle' font-size='10' opacity='0.6'>next</text><rect x='476' y='60' width='54' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><rect x='530' y='60' width='76' height='54' rx='8' fill='none' stroke='rgb(var(--on-track))' stroke-width='1.6'/><text x='503' y='94' text-anchor='middle' font-size='20' font-weight='700' fill='rgb(var(--on-track))'>C</text><text x='568' y='92' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace'>0x9D0</text><text x='541' y='134' text-anchor='middle' font-size='10' font-family='ui-monospace, monospace' opacity='0.55'>@ 0x47C</text></g><g><title>The old B→C link. It isn't 'deleted' — step ② simply writes a new address over it. The danger: if ② runs BEFORE ① copies this address somewhere, 0x47C is destroyed while nothing else holds it, and C plus everything after it is unreachable forever.</title><path d='M160 87 C 264 87, 372 87, 474 87' stroke='rgb(var(--critical))' stroke-width='1.5' fill='none' stroke-dasharray='6 5' opacity='0.85' marker-end='url(#llR)'/><text x='318' y='78' text-anchor='middle' font-size='15' font-weight='700' fill='rgb(var(--critical))'>✕</text><text x='318' y='106' text-anchor='middle' font-size='10' fill='rgb(var(--critical))'>old link 0x47C — overwritten by ②</text></g><g><title>The new node X at 0x9E0, freshly allocated. Its next already holds 0x47C (C's address) because step ① ran first — X is fully wired into the world before the world is wired to X. That order is the entire safety argument.</title><text x='267' y='182' text-anchor='middle' font-size='10' opacity='0.6'>data</text><text x='332' y='182' text-anchor='middle' font-size='10' opacity='0.6'>next</text><rect x='240' y='190' width='54' height='54' rx='8' fill='none' stroke='rgb(var(--accent-fg))' stroke-width='1.8'/><rect x='294' y='190' width='76' height='54' rx='8' fill='none' stroke='rgb(var(--accent-fg))' stroke-width='1.8'/><text x='267' y='224' text-anchor='middle' font-size='20' font-weight='700' fill='rgb(var(--accent-fg))'>X</text><text x='332' y='222' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace'>0x47C</text><text x='305' y='264' text-anchor='middle' font-size='10' font-family='ui-monospace, monospace' opacity='0.55'>@ 0x9E0 · the new node</text></g><g><title>Step ①: X.next = B.next — copy C's address into X while B still remembers it. Nothing observable changes for anyone walking the list; this step is free of risk.</title><path d='M370 217 C 430 217, 478 168, 500 120' stroke='rgb(var(--accent-fg))' stroke-width='1.6' fill='none' marker-end='url(#llS)'/><circle cx='452' cy='196' r='10' fill='none' stroke='rgb(var(--accent-fg))' stroke-width='1.4'/><text x='452' y='200' text-anchor='middle' font-size='11' font-weight='700' fill='rgb(var(--accent-fg))'>1</text></g><g><title>Step ②: B.next = X — the single write that makes X visible. Before it, walkers see B→C; after it, B→X→C. There is no in-between broken state, which is why the order matters so much.</title><path d='M160 92 C 196 106, 204 190, 236 214' stroke='rgb(var(--accent-fg))' stroke-width='1.6' fill='none' marker-end='url(#llS)'/><circle cx='192' cy='156' r='10' fill='none' stroke='rgb(var(--accent-fg))' stroke-width='1.4'/><text x='192' y='160' text-anchor='middle' font-size='11' font-weight='700' fill='rgb(var(--accent-fg))'>2</text></g><text x='318' y='298' text-anchor='middle' font-size='11' font-family='ui-monospace, monospace' fill='rgb(var(--accent-fg))'>① X.next = B.next;   ② B.next = X;   — in that order</text></g>",
          "caption": "Splicing X between B and C is exactly two address writes, and the ORDER is everything. Step ①: point X.next at C (copy B's next, 0x47C, into X). Step ②: point B.next at X (write 0x9E0). Nothing shifted, nothing copied — O(1) however long the list is. Do it backwards and step ② overwrites 0x47C before anything else holds it: C, D, and the whole rest of the chain become unreachable garbage. That's not a slow bug; it's data gone.",
          "slide": "The splice: two writes, one order",
          "id": "linked.f2c4d5e6"
        },
        {
          "type": "trap",
          "body": "**Rewire in the wrong order, lose the list** When inserting after node B, `X.next = B.next` MUST come before `B.next = X`. Write B's next first and the old address is destroyed with nothing else pointing at the rest of the chain — in Java it silently becomes garbage; in a 'what does this code do' exam question it's the intended wrong answer. Same discipline when deleting: save `node.next` before you cut the node out.",
          "source": "Exam material",
          "points": null,
          "id": "linked.aa11bb22"
        },
        {
          "type": "check",
          "prompt": "Why is get(k) O(n) on a linked list when it's O(1) on an array?",
          "answer": "An array's elements sit side by side, so element k lives at a computable address (start + k × size) — one multiplication, one jump. A linked list's nodes are scattered; the ONLY way to find node k is to start at head and follow next k times. The addresses are inside the nodes, so you can't skip ahead.",
          "sectionRef": "linked",
          "id": "linked.c0ffee12"
        },
        {
          "type": "check",
          "prompt": "You're holding a reference to node B in a long singly linked list. What does it cost to insert a node AFTER B? To insert one BEFORE B?",
          "answer": "After B: O(1) — the two-write splice from the figure. Before B: O(n) — you need the node whose next points at B, and in a SINGLY linked list the only way to find that predecessor is to walk from head. (This exact pain is why doubly linked lists exist: a prev pointer makes 'before' O(1) too, at the cost of a third field per node.)",
          "sectionRef": "linked",
          "id": "linked.e5d7a2f0"
        }
      ]
    },
    {
      "id": "invariants",
      "heading": "Loop invariants: proving a loop is right",
      "blocks": [
        {
          "type": "prose",
          "label": "why",
          "md": "**Why anyone bothers** You can run a sort on a thousand arrays and it works every time. Does it work on the thousand-and-first? Testing can only show that bugs *are* there, never that they *aren't*. Poon's definition of **correct**: the algorithm terminates and produces the desired output for *every* valid input. To claim that, you need an argument, not a test. The argument for loops is the **loop invariant**.",
          "slide": "Why testing isn't enough",
          "id": "invariants.38aadffe"
        },
        {
          "type": "prose",
          "md": "**Where you'll meet this after the exam** Invariants are the one proof technique that escapes the classroom. When a reviewer asks 'why does this loop terminate?' or 'can `balance` ever go negative here?', the professional answer is an invariant, stated in one sentence. `assert` statements in production code are invariants the machine re-checks on every run. Database constraints ('total debits equal total credits') are invariants the engine enforces. And when you debug a loop at 2am, the fastest question is: 'what should be true here every single iteration?' — then print exactly that and find the first iteration where it lies to you.",
          "id": "invariants.5b8d02e1"
        },
        {
          "type": "definition",
          "term": "Loop invariant",
          "body": "A sentence about the loop's variables that is true every time the loop is about to check its condition — before the first iteration, between every pair of iterations, and after the last. It describes the *progress so far*, usually as a fact about the prefix the loop has already handled.",
          "id": "invariants.a8982306"
        },
        {
          "type": "prose",
          "md": "If you did the induction section of Chapter 0, you already know this proof shape. **Initialization** = base case: true before the first iteration. **Maintenance** = inductive step: if it's true before one iteration, the body keeps it true for the next. **Termination** = the conclusion: when the loop stops, the invariant plus the stopping condition give you the thing you wanted. Poon's analogy: a Lego tower where every layer built so far is stable and aligned — so the finished tower is too.",
          "slide": "It's induction (Chapter 0) with new names",
          "id": "invariants.a20e63e1"
        },
        {
          "type": "stepper",
          "title": "HW 3 Problem 1: proving the sum loop",
          "frames": [
            {
              "kind": "lines",
              "lines": [
                "sum = 0; i = 0;",
                "while (i < A.length) {",
                "  sum += A[i];",
                "  i++;",
                "}"
              ],
              "active": 0,
              "caption": "HW 3 Problem 1's loop. Goal: prove that when it ends, `sum` is the total of A. First, find a sentence that is true every time the loop is *about to* check its condition."
            },
            {
              "kind": "lines",
              "lines": [
                "Invariant:",
                "at the start of each iteration,",
                "sum = A[0] + A[1] + … + A[i−1]",
                "(the sum of the first i elements)"
              ],
              "active": 2,
              "caption": "The invariant. Notice it's about the *part done so far* (the first i elements), not about the whole array. That's the most common lost point: 'sum is the total of A' is only true at the very end, so it can't be an invariant."
            },
            {
              "kind": "lines",
              "lines": [
                "1. Initialization (i = 0):",
                "sum of the first 0 elements = 0",
                "and sum was set to 0  ✓"
              ],
              "active": 1,
              "caption": "Initialization: before the first iteration, i = 0. The sum of zero elements is 0, and `sum` is 0. True. (This is the base case of an induction proof wearing a different hat.)"
            },
            {
              "kind": "lines",
              "lines": [
                "2. Maintenance:",
                "assume sum = A[0..i−1] before an iteration",
                "body adds A[i]  → sum = A[0..i]",
                "body does i++   → 'A[0..i−1]' now means A[0..i]  ✓"
              ],
              "active": 1,
              "caption": "Maintenance: suppose the sentence is true at the start of some iteration. The body adds A[i], so sum now covers A[0..i]. Then i goes up by one, so 'the first i elements' now means exactly A[0..i]. The sentence is true again. (Inductive step.)"
            },
            {
              "kind": "lines",
              "lines": [
                "3. Termination (i = A.length):",
                "sum = A[0] + … + A[A.length − 1]",
                "= the whole array  ✓"
              ],
              "active": 1,
              "caption": "Termination: the loop stops when i = A.length. Plug that into the invariant: sum is the sum of the first A.length elements — all of them. That's exactly what we wanted to prove. (Conclusion.)"
            }
          ],
          "slide": true,
          "id": "invariants.d1d138d9"
        },
        {
          "type": "example",
          "title": "Four sentences, always the same shape",
          "body": "How do you write a loop-invariant answer so Poon gives full marks?\n\n1. **Invariant:** 'At the start of each iteration with index j, ⟨a property of the part already processed, a[0..j−1]⟩ holds.'\n2. **Initialization:** plug in the first value of j; the property is trivially true (empty or one-element prefix).\n3. **Maintenance:** assume it holds for j; show the loop body extends the property to j+1.\n4. **Termination:** plug in the final value of j; the property now covers the whole array, which is what you wanted.",
          "slide": "The template answer for the exam",
          "id": "invariants.56e4bf15"
        },
        {
          "type": "trap",
          "body": "**The most common lost point** Writing the invariant about the *whole* array ('the array is sorted') instead of the *prefix handled so far* ('a[0..j−1] is sorted'). An invariant has to be true in the middle of the loop, when the job is only partly done.",
          "source": "Exam material",
          "points": null,
          "id": "invariants.992c17d7"
        }
      ]
    },
    {
      "id": "insertion",
      "heading": "Insertion sort",
      "blocks": [
        {
          "type": "prose",
          "label": "why",
          "md": "**The idea before the code** You're dealt cards one at a time. You hold the ones you have in sorted order. Each new card, you slide left past every bigger card until it fits, and drop it in. The cards in your hand are always sorted; the deck is the unsorted part. That's insertion sort: a **sorted prefix** that grows by one each round, and a **key** that walks left into its place.",
          "slide": "Sorting a hand of cards",
          "id": "insertion.914a621c"
        },
        {
          "type": "stepper",
          "title": "Insertion sort on [8, 5, 2, 6, 9] (HW 3 Problem 2)",
          "frames": [
            {
              "kind": "array",
              "cells": [
                8,
                5,
                2,
                6,
                9
              ],
              "done": [
                0
              ],
              "hl": [
                1
              ],
              "note": "j = 1, key = 5",
              "caption": "Start. The first element alone counts as 'sorted' (green). The key is the first unsorted element, 5. We'll walk it left until it's in place."
            },
            {
              "kind": "array",
              "cells": [
                8,
                8,
                2,
                6,
                9
              ],
              "done": [
                0
              ],
              "hl": [
                1
              ],
              "note": "8 > 5 → shift 8 right",
              "caption": "Compare the key with the element to its left: 8 > 5, so copy 8 one slot to the right. There's now a 'hole' at index 0 (still showing the old 8)."
            },
            {
              "kind": "array",
              "cells": [
                5,
                8,
                2,
                6,
                9
              ],
              "done": [
                0,
                1
              ],
              "hl": [
                2
              ],
              "note": "drop key at index 0 · j = 2, key = 2",
              "caption": "Nothing left to compare, so drop 5 into the hole. Sorted prefix is now [5, 8]. Next key: 2."
            },
            {
              "kind": "array",
              "cells": [
                5,
                5,
                8,
                6,
                9
              ],
              "done": [
                0,
                1
              ],
              "hl": [
                2
              ],
              "note": "8 > 2 shift, 5 > 2 shift",
              "caption": "8 > 2 → shift 8. Then 5 > 2 → shift 5. The hole has walked all the way to index 0."
            },
            {
              "kind": "array",
              "cells": [
                2,
                5,
                8,
                6,
                9
              ],
              "done": [
                0,
                1,
                2
              ],
              "hl": [
                3
              ],
              "note": "drop key at index 0 · j = 3, key = 6",
              "caption": "Drop 2 into the hole. Sorted prefix [2, 5, 8]. Next key: 6."
            },
            {
              "kind": "array",
              "cells": [
                2,
                5,
                8,
                8,
                9
              ],
              "done": [
                0,
                1,
                2
              ],
              "hl": [
                3
              ],
              "note": "8 > 6 shift · 5 > 6? no → stop",
              "caption": "8 > 6 → shift 8. Now compare 5 with 6: 5 is not greater, so **stop**. This early stop is what makes insertion sort fast on nearly-sorted input."
            },
            {
              "kind": "array",
              "cells": [
                2,
                5,
                6,
                8,
                9
              ],
              "done": [
                0,
                1,
                2,
                3
              ],
              "hl": [
                4
              ],
              "note": "drop key at index 2 · j = 4, key = 9",
              "caption": "Drop 6 into the hole at index 2. Sorted prefix [2, 5, 6, 8]. Last key: 9."
            },
            {
              "kind": "array",
              "cells": [
                2,
                5,
                6,
                8,
                9
              ],
              "done": [
                0,
                1,
                2,
                3,
                4
              ],
              "note": "8 > 9? no → zero shifts",
              "caption": "8 is not greater than 9, so zero shifts; 9 stays put. The loop has run out of keys. Every element is in the sorted prefix: [2, 5, 6, 8, 9]. That's HW 3 Problem 2's answer, and the trace format Poon grades: key, sorted prefix, array after each iteration."
            }
          ],
          "slide": true,
          "id": "insertion.319e9cca"
        },
        {
          "type": "example",
          "title": "Line by line: `j` is the key's starting index; `i` walks left; the while loop shifts bigger elements right; the last line drops the key into the hole. Project 1 wants this exact algorithm with null / empty guards added.",
          "body": "public static void insertionSort(int[] a) {\n  if (a == null || a.length < 2) return;      // Project 1 guard\n  for (int j = 1; j < a.length; j++) {\n    int key = a[j];\n    int i = j - 1;\n    while (i >= 0 && a[i] > key) {\n      a[i + 1] = a[i];      // shift the bigger element right\n      i = i - 1;\n    }\n    a[i + 1] = key;         // drop key into the hole\n  }\n}",
          "slide": "The code from the slides (Project 1 Part 1)",
          "id": "insertion.12ab85a2"
        },
        {
          "type": "prose",
          "md": "Same algorithm you'd write in TypeScript with `let key = a[j]` — the differences are cosmetic: `int` instead of `let`, `a.length` is the same, `&&` is the same, and there's no `const`. One real difference: Java arrays are fixed size and `int[]` holds only ints, so there's no `.push()`; you index.",
          "slide": "If you're coming from JS/TS",
          "id": "insertion.f8a2c5b5"
        },
        {
          "type": "example",
          "title": "The proof Poon shows",
          "body": "Prove insertionSort is correct.\n\n1. Invariant: at the start of each iteration of the for loop, a[0..j−1] holds the elements that were originally in a[0..j−1], in sorted order.\n2. Initialization: j = 1, so a[0..0] is one element — trivially sorted.\n3. Maintenance: assume a[0..j−1] is sorted. The while loop shifts every element bigger than key one slot right and drops key just before them, so a[0..j] is the original elements of a[0..j], sorted. The invariant now holds for j+1.\n4. Termination: the loop ends when j = a.length. The invariant says a[0..a.length−1] — the whole array — is sorted. Done.",
          "slide": "Insertion sort's own invariant (from the slides)",
          "id": "insertion.f8e4daf8"
        },
        {
          "type": "definition",
          "term": "Best case — already sorted",
          "body": "the while condition `a[i] > key` is false immediately every time, so the inner loop never runs. One comparison per key: **Ω(n)**.",
          "id": "insertion.f9800247"
        },
        {
          "type": "definition",
          "term": "Worst case — sorted backwards",
          "body": "every key must walk past every earlier element. Shifts: 1 + 2 + 3 + … + (n−1) = n(n−1)/2 (Chapter 0's sum). That's about n²/2: **O(n²)**.",
          "id": "insertion.3caf2360"
        },
        {
          "type": "definition",
          "term": "Duplicates",
          "body": "(HW 3 Problem 3): the worst case stays O(n²). Because the test is strict (`>`), equal elements don't shift past each other — which is also why insertion sort is **stable**: equal items keep their original order.",
          "id": "insertion.3e2f4196"
        },
        {
          "type": "prose",
          "md": "**'Why would anyone use an O(n²) sort?' (slide 29 — a likely short answer)** Three reasons. (1) **Small arrays** (under ~20): tiny constant factors and cache-friendly memory beat merge sort. Real libraries switch to insertion sort for small pieces — that is exactly Project 1's hybrid sort. (2) **Nearly-sorted data**: close to O(n) because the inner loop stops early. (3) **Simple and stable.**",
          "id": "insertion.ddb02912"
        },
        {
          "type": "check",
          "prompt": "Trace insertion sort on [4, 3, 2, 1]. How many shifts total, and what does that number tell you?",
          "answer": "j=1 key 3: 1 shift → [3,4,2,1]. j=2 key 2: 2 shifts → [2,3,4,1]. j=3 key 1: 3 shifts → [1,2,3,4]. Total 1+2+3 = 6 = 4·3/2 = n(n−1)/2. Reverse order is the worst case: every key shifts past everything.",
          "sectionRef": "insertion",
          "id": "insertion.12903415"
        },
        {
          "type": "check",
          "prompt": "What input makes insertion sort fastest, and what makes it slowest? Give the growth for each.",
          "answer": "Fastest: already sorted, Ω(n) — the inner loop condition fails at once. Slowest: sorted largest-to-smallest, O(n²) — every element shifts past all earlier ones.",
          "sectionRef": "insertion",
          "id": "insertion.7fe2e577"
        }
      ]
    }
  ],
  "exercises": [
    {
      "id": "stack-queue-story",
      "title": "Which ADT?",
      "prompt": "A text editor's undo feature. Every edit is recorded; pressing Undo reverses the most recent edit that hasn't been undone yet. Which ADT, which operations map to 'make an edit' and 'press Undo', and why not the other one?",
      "choices": [
        {
          "text": "Queue — enqueue on edit, dequeue on undo",
          "feedback": "A queue would undo the *oldest* edit first. Try it: edits A, B, C; dequeue gives A. That's not what Undo does."
        },
        {
          "text": "Stack — push on edit, pop on undo",
          "feedback": "Right. The most recent edit is the first one reversed: last in, first out."
        },
        {
          "text": "List — add at index 0 on edit, get(size−1) on undo",
          "feedback": "A list can be made to work, but you'd be reimplementing a stack with extra steps and an O(n) shift on every edit. Poon's point about ADTs: pick the contract that matches the behaviour."
        }
      ],
      "answer": 1,
      "hints": [
        "Ask the one question: when you undo, do you reverse the *first* edit you ever made, or the *most recent* one?",
        "Students often pick the structure they find more familiar rather than matching the *order* of removal. The only thing that decides stack vs queue is: first-in-first-out or last-in-first-out?",
        "Say the scenario out loud as 'the ___ thing in is the ___ thing out'. Fill the blanks; that sentence names the ADT."
      ],
      "solution": [
        "Undo reverses the most recent edit: last in, first out. That's a stack.",
        "Make an edit = push(edit). Press Undo = pop() and reverse what it returns. Redo is a second stack: each undone edit is pushed there.",
        "A queue would reverse the oldest edit first — wrong behaviour, not just slower."
      ],
      "why": "Undo/redo, the browser's back button, the call stack that runs every function you write, and matching brackets in a compiler are all stacks. Recognizing 'this is LIFO' in a story is a skill interviewers test directly."
    },
    {
      "id": "circular-queue-trace",
      "title": "Trace a circular queue",
      "prompt": "Capacity 4, head = 0, tail = −1, size = 0. Run: enqueue(A), enqueue(B), enqueue(C), dequeue(), dequeue(), enqueue(D), enqueue(E), enqueue(F). After each operation write head, tail, size, and the array. Which enqueue wraps around, and what is `tail` right after it?",
      "hints": [
        "Keep a table with columns: op · array · head · tail · size. Fill one row per operation. Enqueue moves tail; dequeue moves head; both use `(x + 1) % 4`.",
        "The usual slip is forgetting that dequeue does NOT clear or shift anything — it only moves `head`. The old value stays in the slot until something overwrites it.",
        "Wraparound happens when tail is at index 3 and you enqueue: (3 + 1) % 4 = 0. Watch for the moment tail hits 3."
      ],
      "solution": [
        "enqueue A: [A · · ·], head 0, tail 0, size 1. enqueue B: [A B · ·], tail 1, size 2. enqueue C: [A B C ·], tail 2, size 3.",
        "dequeue → A: head 1, size 2. dequeue → B: head 2, size 1. (Slots 0 and 1 still hold A and B physically, but they're free.)",
        "enqueue D: tail (2+1)%4 = 3, [A B C D], size 2. enqueue E: tail (3+1)%4 = **0** — wraps — [E B C D], size 3.",
        "enqueue F: tail 1, [E F C D], size 4 — full. Queue order (from head): C, D, E, F.",
        "The enqueue of E wraps; tail = 0 right after it."
      ],
      "why": "This exact structure is the ring buffer inside every keyboard driver, audio pipeline, and network card — a fixed block of memory reused forever with two indexes. If you ever write anything real-time, you'll write this."
    },
    {
      "id": "invariant-max",
      "title": "State and prove a loop invariant",
      "prompt": "Write the loop invariant for this code and give Initialization, Maintenance, and Termination in Poon's four-sentence format.",
      "code": "int best = a[0];\nfor (int i = 1; i < a.length; i++)\n  if (a[i] > best) best = a[i];",
      "hints": [
        "The invariant is a sentence about `best` and `i` that's true every time the loop is about to test `i < a.length`. What does `best` hold at that moment — in terms of i?",
        "The classic mistake: 'best is the maximum of the array'. That's only true at the END. Mid-loop, best is the maximum of the part you've *seen*. Name that part using i.",
        "Template: 'At the start of each iteration with index i, best = the maximum of a[0..i−1].' Now plug in i = 1 for initialization and i = a.length for termination."
      ],
      "solution": [
        "Invariant: at the start of each iteration with index i, `best` equals the maximum of a[0..i−1].",
        "Initialization: i = 1, so a[0..0] is just a[0], and best was set to a[0]. ✓",
        "Maintenance: assume best = max of a[0..i−1]. The body compares a[i] to best and keeps the larger, so best = max of a[0..i]. Then i increments, so the invariant reads 'max of a[0..i−1]' again for the new i. ✓",
        "Termination: the loop ends when i = a.length. The invariant says best = max of a[0..a.length−1] — the whole array. ✓"
      ],
      "why": "Every time you write a loop that accumulates something (a max, a sum, a running average, 'the best candidate so far'), the invariant is the sentence that tells you the loop is right — and it's the sentence you'll write in a code review comment when someone asks 'why does this work?'",
      "sectionRef": "invariants"
    },
    {
      "id": "insertion-trace",
      "title": "Trace insertion sort",
      "prompt": "Trace insertion sort on [6, 3, 7, 1, 5]. For each outer iteration write the key, the sorted prefix before the insert, the number of shifts, and the array after. Then: how many shifts total, and what input of length 5 would give the maximum possible?",
      "hints": [
        "Set up five rows, one per j from 1 to 4. The key is a[j]; the sorted prefix is a[0..j−1]; a shift happens every time the element left of the hole is bigger than the key.",
        "The common mistake is counting comparisons instead of shifts, or forgetting that the loop stops at the first element that is NOT bigger than the key (the `>` is strict).",
        "For the maximum: what arrangement forces every key to walk past every earlier element? Then use 1 + 2 + … + (n−1)."
      ],
      "solution": [
        "j=1 key 3, prefix [6]: 6 > 3 → 1 shift → [3, 6, 7, 1, 5].",
        "j=2 key 7, prefix [3, 6]: 6 > 7? no → 0 shifts → [3, 6, 7, 1, 5].",
        "j=3 key 1, prefix [3, 6, 7]: 7, 6, 3 all bigger → 3 shifts → [1, 3, 6, 7, 5].",
        "j=4 key 5, prefix [1, 3, 6, 7]: 7 > 5 shift, 6 > 5 shift, 3 > 5? no → 2 shifts → [1, 3, 5, 6, 7].",
        "Total shifts: 1 + 0 + 3 + 2 = 6. Maximum for n = 5: reverse order [5, 4, 3, 2, 1] gives 1 + 2 + 3 + 4 = 10 = n(n−1)/2."
      ],
      "why": "Insertion sort is what real libraries run on small or nearly-sorted pieces (Python's sort, Java's, and your Project 1 hybrid). Knowing exactly what it does on a specific input is how you predict whether it'll be fast on *your* data.",
      "sectionRef": "insertion"
    }
  ]
}
