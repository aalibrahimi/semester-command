{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main window. Deliberately narrow: this app reads Canvas from Rust and writes only to the local database, so the webview needs no filesystem, shell or HTTP permission of its own.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-start-dragging",
    "notification:default",
    "os:allow-os-type",
    "os:allow-platform",
    "opener:allow-open-url"
  ]
}
